# Base URL the API is served from, no trailing slash.
# Local dev: leave empty — Vite's dev proxy (vite.config.ts) forwards /api to localhost:4000.
# Production (e.g. API deployed as a separate Render service): set to that service's public URL.
VITE_API_BASE_URL=
