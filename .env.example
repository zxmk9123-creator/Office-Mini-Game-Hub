DATABASE_URL=postgres://localhost:5432/mini_game_hub
PORT=4000

# Comma-separated list of origins allowed to call this API.
# Local dev: leave unset — defaults to http://localhost:5173 (Vite's dev server).
# Production: set to the deployed web app's real origin(s).
CORS_ORIGIN=
